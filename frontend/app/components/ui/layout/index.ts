/**
 * Layout - Layout and structural components
 * Section headers, overlays, and page structure elements
 */

export { default as SectionHeader } from './SectionHeader';
export { default as GradientOverlay } from './GradientOverlay';
